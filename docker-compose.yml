services:
  ubuntu:
    container_name: ubuntu
    build:
      context: .
      dockerfile: Dockerfile
      cache_from:
        - type=local,src=./docker-images,ignore-error=true
      cache_to:
        - type=local,dest=./docker-images
